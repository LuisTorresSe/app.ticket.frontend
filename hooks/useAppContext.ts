
// This file is kept for structural convention, but its logic is now inside context/AppContext.tsx
// to avoid circular dependency issues when the context needs to use its own action creators.
// The useAppContext export from context/AppContext.tsx should be used instead.

export {};
// See context/AppContext.tsx for the implementation of useAppContext
